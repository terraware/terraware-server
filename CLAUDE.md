# Coding conventions for terraware-server

Always include a trailing newline when working on mjml, html, and ftl files.

## Tools

To check code style: `./gradlew spotlessCheck`

To verify compilation: `./gradlew testClasses`
