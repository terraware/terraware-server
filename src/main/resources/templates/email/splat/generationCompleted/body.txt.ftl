<#-- @ftlvariable name="" type="com.terraformation.backend.email.model.SplatGenerationCompleted" -->
${strings("notification.splat.generationCompleted.email.body.1")}

${strings("notification.splat.generationCompleted.email.body.2")}

${walkthroughUrl}

------------------------------

${strings("notification.email.text.footer", manageSettingsUrl)}
