<#-- @ftlvariable name="" type="com.terraformation.backend.email.model.ApplicationAccepted" -->
${strings("notification.application.accepted.email.title")}

${strings("notification.application.accepted.email.body")}

${strings("notification.application.accepted.email.buttonLabel")}

${applicationUrl}


------------------------------

${strings("notification.email.text.footer", manageSettingsUrl)}
