<#-- @ftlvariable name="" type="com.terraformation.backend.email.model.ApplicationNotAccepted" -->
${strings("notification.application.notAccepted.email.title")}

${strings("notification.application.notAccepted.email.body")}

${strings("notification.application.notAccepted.email.buttonLabel")}

${applicationUrl}


------------------------------

${strings("notification.email.text.footer", manageSettingsUrl)}
