<#-- @ftlvariable name="" type="com.terraformation.backend.email.model.ScheduleObservationReminder" -->
${strings("notification.observation.scheduleReminder.email.body.1")}

${strings("notification.observation.scheduleReminder.email.body.2", plantingSiteName)}

${observationsUrl}


------------------------------

${strings("notification.email.text.footer", manageSettingsUrl)}
