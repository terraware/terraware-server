<#-- @ftlvariable name="" type="com.terraformation.backend.email.model.ObservationScheduled" -->
${strings("notification.observation.scheduled.email.body.1", organizationName, plantingSiteName)}

${strings("notification.observation.scheduled.email.body.2", plantingSiteName, startDateString, endDateString)}

------------------------------

${strings("notification.email.text.footer", manageSettingsUrl)}
